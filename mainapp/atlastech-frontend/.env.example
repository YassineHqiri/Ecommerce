# API base URL (used at build time).
# - If frontend & backend share the same domain (e.g. both at example.com, API at /api), leave empty.
# - If API is on a different domain (e.g. api.example.com), set:
#   VITE_API_URL=https://api.example.com/api
# VITE_API_URL=
